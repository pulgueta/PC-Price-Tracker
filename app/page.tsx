import { Button } from '@/components/ui/button';

const Home = () => {
	return (
		<main className='flex h-dvh flex-col items-center justify-center gap-4'>
			<Button>First commit</Button>
		</main>
	);
};
export default Home;
